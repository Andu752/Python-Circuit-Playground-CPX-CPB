# Open the file in read mode
with open('text-file-matrix.txt', 'r', encoding='UTF-8') as file:
    # Read and print the contents of the file
    for line in file:
        print(line.strip())  # strip() removes trailing newline characters

# File is automatically closed when using 'with' statement
